import React from "react";
import { Text, View } from "react-native";

const Login = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

Login.defaultProps = {};

export default Login;
